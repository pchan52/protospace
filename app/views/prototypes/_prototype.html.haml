.col-sm-4.col-md-3.proto-content
  .thumbnail
    = link_to image_tag("sample.png"), prototype_path(prototype.id)
    .caption
      %h3
        = prototype.name
      .proto-meta
        .proto-user
          = link_to "by #{prototype.user.name}", user_path(prototype.user.id)
        .proto-posted
          = prototype.created_at.strftime('%m/%d')